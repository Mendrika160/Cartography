//show marker on map 

/*{places.map((place,index) => { 
    <Marker key={index} position={place.geometry.coordinates} icon={defaultIcon}>
                        <Popup>{place.properties.place}</Popup>
      </Marker>
  })}
      
  */


// show places.json data on map
  /***
       {data.places.map(place => { 
          <Marker key={place.id} position={place.coordinates} icon={defaultIcon}>
            <Popup>
              {place.name}
            </Popup>

          </Marker>

        })}
   * 
   * 
   */

    /*

    <Marker position={[-21.454799,47.086746]} marker={defaultIcon}>
            <Popup>
              Zomatel
            </Popup>

          </Marker>
    */